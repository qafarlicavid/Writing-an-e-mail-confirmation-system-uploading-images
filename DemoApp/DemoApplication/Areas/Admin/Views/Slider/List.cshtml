@model List<ListItemViewModel>

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-controller="slider" asp-action="add" target="_blank" class="btn btn-secondary">Add new Slider</a>
        </div>
    </div>
</div>
<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Sliders</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Content</th>
                            <th>BackgroundImageUrl</th>
                            <th>Created at</th>
                            <th>Updated at</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var slider in Model)
                        {
                            <tr>
                                <td>@slider.Title</td>
                                <td>@slider.Content</td>
                                <td>@slider.BackgroundImageUrl</td>
                                <td>@slider.CreatedAt.ToString("dd/MM/yyyy")</td>
                                <td>@slider.UpdatedAt.ToString("dd/MM/yyyy")</td>
                                <td class="text-end">
                                    <a asp-route="admin-slider-update" asp-route-id="@slider.Id" class="btn btn-success" >Update</a>
                                    <form method="POST" asp-route="admin-slider-delete" asp-route-id="@slider.Id" id="slider-form-@slider.Id" ></form>
                                    <button class="btn btn-danger"  type="submit" form="slider-form-@slider.Id">
                                        Delete
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>